import type { BlogPost } from '../../data/types'

export const nanoNotebookDevLog10 = {
  slug: 'nano-notebook-dev-log-10',
  title: 'nano-notebook 开发日志 10：长任务里的上下文该怎么管',
  date: '2026-09-18',
  excerpt:
    '从全量历史与临界摘要出发，讲清 AgentStatus、工具结果外置和分层压缩怎样让长任务持续推进，以及固定的 Research Plan 为什么必要。',
  content: [
    "**理解是上下文管理的宗旨。**",
    "最直接的上下文管理方式，是把用户消息、Agent 的每次工具调用和完整结果都留在对话里。模型每回合都能看到过去发生的事；窗口快满时，再把旧内容压成一段摘要，保留当前目标与已经完成的工作，也交代接下来要做的事和仍须遵守的规则。",
    "这个办法能让任务继续跑，却会丢细节。一次工具调用可能返回一整篇文章，摘要只记下“读过这篇文章，结论是……”。如果报告后来需要核对原文里的一个限定条件，摘要未必还记得。更长的任务里，模型也可能只盯着最近一次工具结果，慢慢忘掉这次工作究竟要解决什么。",
    "## 每一回合都要知道自己做到哪了",
    "想象一个人正在写技术调研。他读完几篇材料，不会在写下一段前把所有材料从头看一遍。他会先看自己的笔记：这次要回答什么，哪些判断已经有依据，哪里还没有查清，下一步应该读什么。需要引用某段原文时，再翻回那份材料。整理笔记并没有让他忘掉工作，反而让他腾出精力继续判断。",
    "Agent 也需要这样的工作状态。上下文管理要帮助模型理解输入如何改变了任务，而不能只等它忘记以后，再把过去的记录塞回来。一次搜索没有找到证据，下一步就该换查询；读到相互矛盾的材料，下一步就该核对分歧。任务是否在推进，取决于这些变化有没有留下来。",
    "nano-notebook 为此加入了 AgentStatus。模型可以用 `rewrite_todo_list` 建立或改写当前 TODO，用 `update_todo_status` 更新进度。计划内容由模型决定；运行时代码负责检查更新是否合法，并根据已接受的工具结果重建当前状态。每次模型决策前，运行时都会把最新 TODO 和工具调用次数，连同重复调用提示与最近的安全错误等信息整理成一条临时消息，放在请求末尾。",
    "这里的“每回合更新”，指的是运行时每回合重新生成 AgentStatus；模型在计划确实发生变化时更新 TODO。AgentStatus 本身不写进聊天历史，也不拿去压缩。TODO 的更新则记录在 checkpoint 中，同一用户输入发生重试时可以恢复。这样，提醒始终在模型最近看到的位置，计划也不会因为一次请求结束就消失。",
    "## 把大结果移出上下文",
    "工作状态清楚以后，仍要给后续步骤腾位置。压缩时要先看过去的一步让当前任务发生了什么变化。工具名称、调用参数和成功或失败，能交代 Agent 做过什么。内容性的结果还要留下关键结论、约束和未解决的分歧，否则模型只知道自己读过材料，却不知道读出了什么。",
    "占空间最多的往往是工具返回的正文。假设 Agent 读了一本书的某一节，眼下只需要记住它支持了哪项判断，整节文字没必要在之后每回合都重发。nano-notebook 会把过大的只读工具结果外置，在上下文里留下有界预览和引用。需要核对具体段落时，Agent 可以按引用读取有限范围；临时缓存过期后，仍需回到持久化的来源重新取得内容。引用本身不能代替原文的长期保存。",
    "这个区别在数据里很直观。项目的一项长工具结果测试中，约 510 KB 的原始结果经过外置后，模型可见的结果约为 51 KB，减少了约 90% 的结果字节。这是单条结果的样本，不能当作整次任务都能压掉 90% 的保证。",
    "## 旧步骤继续压，最近步骤留原样",
    "工具结果外置以后，长程研究仍可能积累大量步骤。nano-notebook 的 Research Mode 会在上下文接近预算时处理较旧的完整 Agent Step，同时保留最近一段精确历史。它不会把一次工具调用和对应结果拆开。",
    "第一层是刚才说的结果外置。完整内容留在对应存储或缓存，模型只看有界表示。第二层把旧步骤整理成逐步的 Capsule，记录那一步推进了什么目标、形成了哪些判断、留下了什么问题；工具调用参数和结果状态仍可在这一层的投影中看到。如果这些 Capsule 积累起来还是太长，第三层再把更早的一段汇成 Research Task Memory，保留与当前任务有关的结论、决策和待办线索。",
    "压缩改变的是发送给模型的历史表示，原始 checkpoint 不会被删掉。新一轮请求还会重新生成 AgentStatus。于是，模型眼前有近期的精确步骤、较早工作的浓缩结果，以及当前计划；需要精确数据时再循来源和引用查回去。普通 Chat 有自己的上下文摘要机制，上述三层是 Research Mode 的处理路径。",
    "## 原目标不能跟着工作笔记一起漂移",
    "AgentStatus 仍有一个边界。TODO 由模型更新，任务跑得越久，模型越可能把自己的阶段性判断当成新目标；后来每次更新都能接上前一次，看起来连贯，实际已经离开最初的问题。只让模型复述自己的计划，挡不住这种偏离。",
    "长程任务因此需要一份模型不能自行改写的目标。在 nano-notebook 的 Research Mode，用户确认后的 Research Plan 会作为固定版本进入执行上下文。它写明目标与范围，也列出预期章节和完成条件；模型可以调整自己的 TODO 和调查路径，却不能在执行中悄悄重写这份已确认的计划。报告交付前，还要拿它检查内容是否覆盖了约定的问题。当前运行时能固定计划版本、约束发布和引用边界；“报告确实回答了所有问题”仍需要审阅，不能只凭 Agent 自己宣布完成。",
    "长任务要持续推进，既要把占空间的历史整理掉，也要让 Agent 每回合知道当前工作的位置。AgentStatus 负责眼前的进度，分层压缩保留过去工作带来的变化，外置结果允许必要时回查原文，固定的 Research Plan 则守住任务起点。",
  ],
  aiDisclosure:
    '本文由 AI 协助整理表达；项目实现以 nano-notebook 当前代码和架构文档为准。',
  readingMinutes: 5,
  category: 'software',
  topic: 'agent-architecture',
  series: 'nano-notebook-dev-log',
  tags: ['nano-notebook', 'Agent', 'Context Engineering', 'AgentStatus'],
  status: 'published',
} satisfies BlogPost
