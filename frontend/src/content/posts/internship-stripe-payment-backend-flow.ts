import type { BlogPost } from '../../data/types'

export const internshipStripePaymentBackendFlow = {
  slug: 'internship-stripe-payment-backend-flow',
  title: '实习总结：Stripe 支付后端链路的抽象理解',
  date: '2026-05-18',
  excerpt:
    '整理一次 Stripe 支付后端链路的核心逻辑：创建支付对象、等待 webhook、校验结果、映射业务、幂等兑现。',
  content: [
    '最近在实习里看了一套 Stripe 支付相关的后端实现。刚开始看支付代码时，很容易被具体接口、字段名、业务表和项目命名带着走，但把这些细节剥掉之后，它其实是一条很清晰的后端链路。',
    '我现在对 Stripe 支付的理解，可以压缩成一句话：Stripe 负责完成支付，后端负责可信确认和业务兑现。',
    '## 发起支付不是支付成功',
    '用户点击购买、充值或订阅时，后端首先做的不是修改业务状态，而是向 Stripe 创建一个支付对象。这个对象可以是 Checkout Session、Payment Intent，也可以是 Subscription 相关对象，具体形态取决于是一次性支付还是订阅支付。',
    '这个阶段的后端职责，是把本地业务里的用户和商品翻译成 Stripe 能理解的支付请求。比如要收多少钱、用什么币种、支付成功后跳到哪里、取消后跳到哪里，以及这笔支付属于哪个本地用户或哪张本地订单。',
    '但这里有一个非常重要的边界：创建支付对象只说明“可以去支付了”，不说明“已经支付了”。所以在这个阶段，后端最多记录一条支付请求日志，然后把支付链接或 client secret 返回给前端。余额、会员、订单状态这些真正的业务权益，都不应该在这里发放。',
    '## 成功页不是可信依据',
    '支付完成后，用户可能会被 Stripe 重定向回前端成功页。这个成功页对用户体验很重要，但它不能作为后端确认付款的依据。',
    '原因很简单：前端跳转可以失败，可以被刷新，也可以被伪造。真正可信的支付结果，应该来自 Stripe 发给后端的 webhook。后端收到 webhook 后，第一件事不是处理业务，而是校验签名，确认这个请求确实来自 Stripe，并且内容没有被篡改。',
    '这一步建立的是信任边界：用户浏览器告诉我的不算，Stripe 签过名的事件才算。',
    '## Webhook 事件驱动业务分流',
    '验签通过后，后端再根据 webhook 的事件类型判断发生了什么。一次性支付成功、订阅创建成功、订阅续费成功、支付失败、退款、订阅取消，本质上都是不同事件。',
    '这一步不应该写成“收到 webhook 就加钱”这种粗糙逻辑。后端需要先识别事件语义，再决定对应的业务动作。一次性支付成功可能意味着订单改成已支付，或者余额增加；订阅付款成功则意味着会员周期开始或延长；退款成功可能意味着撤销权益、写退款流水或标记订单异常。',
    '所以 webhook 处理函数的核心不是 Stripe SDK 调用，而是事件到业务动作的映射。',
    '## 从 Stripe 对象映射回本地业务',
    'Stripe 的世界里有 customer、session、payment_intent、price、invoice、subscription；业务系统自己的世界里有用户、订单、商品、套餐、余额、会员周期。',
    '支付成功后，后端必须把 Stripe 对象映射回本地对象。常见做法是在创建支付对象时，把本地用户 ID、订单 ID、商品 ID 或套餐 ID 写进 metadata；处理 webhook 时再从 metadata 或 Stripe 对象关系里拿回来。',
    '对一次性支付来说，映射通常比较直接：找到本地用户或订单，确认支付金额，然后发放对应权益。',
    '对订阅来说，映射会更复杂一些：后端通常需要根据 Stripe price id 找到本地套餐，再根据 subscription 或 invoice 判断当前是哪一个计费周期，最后更新会员状态、周期结束时间和周期权益。',
    '这里最容易混淆的是：Stripe 的 price id 不是业务本身，它只是业务商品或套餐在 Stripe 里的价格标识。真正要落到系统里的，仍然是本地定义的业务概念。',
    '## 业务兑现必须幂等',
    'Webhook 有一个非常关键的现实前提：它可能重复发送。Stripe 为了保证事件最终送达，会在网络失败、超时或后端返回异常时重试。同一笔支付，后端可能收到多次相同事件。',
    '所以支付后端一定要做幂等。处理成功前，先检查这条 Stripe event、checkout session、payment intent、invoice 或 subscription cycle 是否已经处理过。如果已经处理过，就直接返回成功或忽略，不能重复发放权益。',
    '幂等不是优化，而是支付系统的基本安全要求。没有幂等，一次 webhook 重试就可能导致重复加余额、重复开会员、重复发放额度。',
    '## 流水比状态更重要',
    '支付系统不能只改一个最终状态。比如订单从未支付变成已支付，或者用户余额从 0 变成 100，这些状态当然重要，但它们不够解释“为什么变成这样”。',
    '后端还需要记录支付请求日志、webhook 处理日志、交易流水、余额流水、会员变更记录。这样出了问题时，才能回答几个关键问题：当时创建了哪个 Stripe 对象，Stripe 发来了什么事件，这个事件是否验签成功，业务是否处理成功，权益到底发放了几次。',
    '支付链路天然需要对账和排错。流水不是附属品，而是系统可解释性的来源。',
    '## 一次性支付和订阅支付的差异',
    '一次性支付可以理解成一笔明确交易：用户付一笔钱，系统交付一次权益。它的后端链路通常是 payment success 到 order paid，或者 payment success 到 balance increased。',
    '订阅支付表达的是周期性关系。它不只是“这次付钱成功”，还意味着用户进入了某个套餐周期，未来可能续费、取消、升级、降级、支付失败或退款。',
    '所以订阅后端逻辑通常比一次性支付更像状态机：当前套餐是什么，当前周期什么时候结束，续费成功后如何延长，支付失败后如何处理宽限期，取消后权益到什么时候停止。这些都不应该只靠一次 checkout 成功来表达。',
    '## 我最终记住的链路',
    '把所有细节抽掉之后，我会这样记 Stripe 支付的后端逻辑：',
    '```text\n用户发起购买\n -> 后端创建 Stripe 支付对象\n -> 前端引导用户完成支付\n -> Stripe webhook 通知后端\n -> 后端校验 webhook 签名\n -> 后端识别事件类型\n -> 后端映射本地用户和业务对象\n -> 后端做幂等检查\n -> 后端发放权益或更新状态\n -> 后端记录流水和处理结果\n```',
    '这条链路里，最重要的不是某个具体接口怎么写，而是职责边界：发起支付阶段只创建支付对象，确认支付阶段只相信 webhook，兑现权益阶段必须先映射业务再保证幂等，最后用流水把整件事记录清楚。',
    '理解了这条边界之后，再看具体代码时就不会被字段名带偏。无论业务是订单、余额、会员还是额度，本质上都只是“支付成功后本地系统要兑现什么权益”的不同表现。',
  ],
  aiDisclosure: '本文由 AI 协助整理表达，内容来自个人实习中的代码阅读和技术总结。',
  readingMinutes: 5,
  category: 'software',
  tags: ['实习总结', 'Stripe', '支付系统', '后端'],
  status: 'published',
} satisfies BlogPost
